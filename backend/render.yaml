services:
  - type: web
    name: ai-news-backend
    env: python
    buildCommand: "pip install -r requirements-web.txt"
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0